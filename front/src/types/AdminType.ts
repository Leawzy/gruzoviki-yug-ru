export interface AdminTypeUser {
    total: string | number;
    users: {
        total: string | number;
    };
}

export interface AdminTypeBrand {
    total: string | number;
    users: {
        total: string | number;
    };
}
